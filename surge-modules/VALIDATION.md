# 验证记录

日期：2026-09-06。环境：Surge Mac 6.9.0（12250）、macOS 26.6.2、原生 JavaScriptCore；Cookie 捕获使用已登录的 Codex 内置浏览器。

## 真实站点结果

| 模块 | 浏览器捕获 | 原生脚本结果 | 限制 |
| --- | --- | --- | --- |
| DeepFlood | 已验证，含浏览器 User-Agent | 无签名最小请求领取固定 5 鸡腿成功；最终脚本读取今日记录并跳过重复领取 | 未引入签名适配 |
| NodeSeek | 已验证，含浏览器 User-Agent | 官网领取固定 5 鸡腿后，最终脚本从服务端记录确认今日已完成 | 本次原生验证为已签到检查 |
| 科研通 | 已验证，完整 Cookie 和 User-Agent 已写入 | 服务端明确返回今日已经签到，新脚本正确识别 | 本次未重复领取 |
| 漫画签到 | 已验证，网页账号凭据与旧存储兼容 | `platform=ios` 返回 `code=1`、不能重复签到，新脚本正确识别 | 不要求手机 App 捕获 |
| 漫画积分抢购 | 共用漫画凭据 | 只读查询成功：55 积分、目标商品单价 100、库存 0 | 未执行真实兑换，未消费积分 |
| V2EX | 已验证 `A2` 和对应 User-Agent | 领取成功；重读网页确认已领取；再次执行返回今日已完成 | 凭据与临时令牌未写入仓库 |

## 已验证的兼容修复

- Surge 请求超时按秒设置；JavaScriptCore 中仅在 `clearTimeout` 可用时调用。
- 通知失败也必须到达 `$done()`；测试覆盖通知故障和缺少 `clearTimeout`。
- Seek 捕获支持实际 Service Worker 的本站来源，保留个人资料及账号 ID 校验。省略空正文和表单 Content-Type；2026-09-07 已纠正此前同时省略 Origin 的处理，POST 必须保留同源 Origin。先读取今日记录以避免重复提交，不使用 Refract 签名或密钥更新逻辑。
- 科研通识别“今天已于某时间签到”的真实响应，不能仅根据 `code=1` 判断重复。
- 漫画使用 API 要求的 `platform=ios`，试运行不调用 Exchange，结果不确定时不自动重发交易。

## 发布检查

六个模块、11 项脚本声明和四个自包含 JS 的路径归属及语法校验已通过，六份逐模块配置均由 Surge 原生检查返回 OK。五个普通签到默认每天 00:05，兑换模块保留原时段。完整离线测试 `node --test tests/*.test.js` 共 94 项通过，包含原 Boil 模块回归。代码语法、兼容入口同步和空白检查均通过。

发布后须核对远程原始文件与本地内容一致，并确认本机仅启用新来源、普通签到 cron 为 `5 0 * * *`。本机操作及每次发布结果保存在对应 Trellis 任务记录中。

## 2026-09-07 回归诊断与修复

00:05 的五个定时任务均已触发。科研通和漫画成功；V2EX 正确识别上一奖励日已领取。两个 Seek 在领奖前被本地状态校验阻止：真实未签到响应是 `record=null、order=null`，原测试错误使用 `order=-1`，遗漏了真实空排名分支。已补充先失败后通过的回归测试。

修正空排名后，DeepFlood 原生签到成功。NodeSeek 仍返回 HTTP 403 和 `high risk action`；两站使用相同代理出口，更新 Cookie 也未解决。保持请求其他部分不变，仅补充本站 `Origin` 后，NodeSeek 原生签到成功。正式脚本保留这一标准来源头，不增加独立签名架构或自动重试。

通知缺失另有两层原因：Surge 本地通知开关关闭，macOS 也未授权 Surge 发送通知。两处现已开启，用户截图确认签到成功、失败及 Cookie 更新通知均能显示。

本次同时加入真实余额显示，并统一通知的标题、副标题和字段标签。五个站点均通过 Surge CLI 查询到真实余额；V2EX 只解析当前任务页已有余额。测试覆盖合法零余额、缺失余额、余额账号不匹配和余额查询失败时保留正确签到状态，避免虚构余额。

本轮完整回归 `node --test tests/*.test.js` 共 106 项通过，包含原 Boil 模块回归。正式脚本与旧科研通入口语法检查通过，模块依赖校验通过；执行时间及模块参数未改变。
